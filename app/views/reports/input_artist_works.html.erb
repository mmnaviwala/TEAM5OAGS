
<h1>Artist Works Report</h1>
  <div class="row" >
    <div class="span6 offset3">
	  <div class="actions">
		  <%= form_tag reports_artist_works_path, method: 'get' do %> 

		  	<%= text_field_tag :name, params[:name], :placeholder => "Search by Artist Name ..." %>
			<%# collection_select(@artist_id, :artist_id, Artist.all, :id, :id) %>

		  	<%= submit_tag "Submit!", :name => nil, class: "btn btn-large btn-primary"  %>
		  <% end %>
	  </div>
  </div>
</div>